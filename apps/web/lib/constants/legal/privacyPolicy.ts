import { PrivacyPolicyProps } from "@/lib/ts-types/legal";
import { supportEmailAddress,siteName,companyLegalName,country, websiteUrl,lastUpdated } from "../appDetails";

export const privacyPolicy:PrivacyPolicyProps = {
    supportEmailAddress,
    siteName,
    companyLegalName,
    country,
    websiteUrl,
    lastUpdated
}